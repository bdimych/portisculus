ПЛАН:

	OK -корректное округление bpm из soundstretch
			
	OK -в первом проходе не останавливаться на ручной подсчёт
	OK -после первого прохода сохранить базу

	OK -сделать cygpath чтобы в базу можно было писать каталоги скопированные пути из винды
	
	OK -попробовать сделать в ruby посимвольно читать команду
		в ruby есть модули повторяющие функции stty - см. гугл
			-не, там надо termios какой-то модуль
			а у меня в цигвине gem-а нету
		OK -сделать def readChar(array possibleChars)
	
	OK -перед вторым проходом по подсчёту спрашивать продолжения
	
	-перед каждым ручным подсчёта пауза и статистика что обработано X from Y и что next/stop and save
	-отлавливать ctrl-c
		НЕ -выводить статистику и спрашивать что осталось столько то, выйти ?
			ненадо, at_exit отлично справляется
		OK -при выходе сохранять базу
		-вокруг `count` сделать
			trap("INT") {wasCtrlC = true}
			`count`
			if (wasCtrlC) выход
	

такс, задачки
	-не сохранять базу лишний раз
		сделал
			OK -at_exit { writeDb }
				этот и ctrl-c и raise отлавливает
			OK -после первого прохода
			-ещё наверно при ручном подсчёте можно и каждый раз сохранять
	





	
--help
	1-create-bpm-database.rb
	count-bpm-by-hands.sh
	




-документация


	

	
	
	
	
	
