


дальше надо
	OK -вынуть bpm из аутпута soundstretch
	-а если неполучилось то предлагать
		OK -пропустить файл
		-посчитать вручную

		
итак count-bpm-by-hands.rb
какой алгоритм?
	-написать что мол сейчас запустится воспроизведение и нажимайте
		-пробел для счёта
		-ввод для остановки
		-и ещё что-нибудь для redo?
	или же нетак
	-можно ведь запустить оконный проигрыватель и в нём выбирать место где ритм отчётливый
		-но тогда блин неудобная путаница с окнами - пробел-то надо нажимать в окне терминала
		или как иначе? хук системный чтоли вешать
	-или запускать проигрыватель в фоне
		-но тогда наоборот придёцца самому отлавливать стрелки и перематывать что вообще непонятно как
			-а! вроде бы кстати vlc умеет типа быть сервером и ему действительно можно команды в терминал слать
			-вот mplayer умеет http://ubuntuforums.org/showthread.php?t=1629000
					mkfifo /tmp/mplayer-control
					mplayer -slave -input file=/tmp/mplayer-control /path/to/some/file/to/play
				mkfifo в цигвине вряд ли сработает для виндовского mplayer-а
				но наверно он может и stdin читать - надо почитать mplayer -slave
	-или вообще найти модуль какой-нибудь который умеет воспроизводить
	
в принципе контроль через терминал работает и в vlc ив mplayer
в любом из этих случаев надо научиться читать из терминала посимвольно не ожидая ввода
...вроде вспоминаю собсно в баше у меня получалось вроде бы read там параметр какой-то есть сколько символов читать
	а, ну вот, read -n 1 работает
		да, но однако стрелки это не один символ а вроде как три
		
ЛАДНО, спать пора, завтра соображу
		
		
		
		
		
		
		
		
		

-идея: если в базе вместо bpm например 'count by hands' то запускать ручной счёт
	т.е. управлять скриптом (1-create-bpm-database.rb) не из командной строки а через базу

