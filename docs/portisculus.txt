ПЛАН:

	?-пропускать каталоги если skip???
		это вопрос т.к. если надо пропустить то непонятно а зачем его вообще было добавлять?

	OK -после byhands если bpm пустой то предложить skip
	
	OK -$dbStat считать собственно в самой dbStat
		НЕТ, не надо, лучше в нужных местах
			НЕТ, нужно посложнее:
			нужно сделать
				-в dbStat действительно считать статистику
					(-возврщать не строку а хэш)
				НО не делать впециальную $dbStat а
				-хранить статистику прямо в db
				а для этого
				-сделать def dbAdd path
					в котором и расставлять эти статистические ключи
				и ещё сделать
				-def dbSet path, key, val
					чтобы в ней централизованно ставить $dbChanged
		------- НЕТ ------------
		в итоге всего две осталось
		def dbStat
		и
		def dbSet path, key, value
	
	-в byhands НЕВИДНО имени файла
		надо бы показывать где нибудь на видном месте
	-в byhands показывать где
		-или же весь путь к файлу
			но в этом случае логично не захардкоживать tmp.mp3 а передавать файл параметром и уже в byhands его копировать в tmp3
				кстати можно и не копировать
				ведь >() выполняется в субшелле
				т.е. тогда можно сделать
				>(cd $(dirname); mplayer $(basename))
					ну да, а в подсказке так и показывать /cygdrive/c/Users/bdimych/Downloads/Dune - Discography 1995-2003/Forever And Ever (1998)/03. Almaz.mp3 >
		
	
	

такс, задачки
	OK -не сохранять базу лишний раз
		сделал
			OK -at_exit { writeDb }
				этот и ctrl-c и raise отлавливает
			OK -после первого прохода
			OK -ещё наверно при ручном подсчёте можно и каждый раз сохранять
	





	
--help
	1-create-bpm-database.rb
	count-bpm-by-hands.sh
	




-документация


	

	
	
	
	
	
