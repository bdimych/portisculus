


count-bpm-by-hands.sh
	OK -запускать mplayer на паузе
	-действия:
		OK -space - счёт
			считать и показывать bpm динамически
		OK -q - выход без bpm
			пауза + подтверждение
		d - done - выход с bpm
			пауза + подверждение
		OK r - reset counter
		OK -p - pause
		OK -home/left/right - seek 0/-10/+10 seconds
	
		
-ТАК:
	OK -копроцесс НЕНУЖЕН т.к. вручную всё равно точность после запятой не достичь такой точности
	-и надо всё-таки попробовать сделать очистку строки
		тогда можно будет печатать bpm в процессе подсчёта красиво на одной строке
		-не, кстати ненадо!
			если на каждой строке то видно изменение bpm в динамике
			и лучше по "d" предлагать кроме y|n ещё вручную скорректировать
		
		

-идея: если в базе вместо bpm например 'count by hands' то запускать ручной счёт
	т.е. управлять скриптом (1-create-bpm-database.rb) не из командной строки а через базу


	
	
